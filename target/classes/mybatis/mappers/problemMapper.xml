<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="problemDB">




	<!-- select 쿼리 (여러개) -->
	<select id="selectProblemList"   resultType="ProblemVO">
		select place_code,problem.code,problem_detail,place_detail 
		from storecare.problem, storecare.place 
		where problem.place_code=place.code
	</select>
	
</mapper>